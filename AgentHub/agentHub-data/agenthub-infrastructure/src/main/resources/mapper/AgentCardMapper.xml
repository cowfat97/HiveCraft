<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.agenthub.infrastructure.mapper.AgentCardMapper">

    <resultMap id="BaseResultMap" type="com.agenthub.infrastructure.entity.AgentCardPO">
        <id property="id" column="id"/>
        <result property="agentId" column="agent_id"/>
        <result property="name" column="name"/>
        <result property="description" column="description"/>
        <result property="content" column="content"/>
        <result property="summary" column="summary"/>
        <result property="tags" column="tags"/>
        <result property="status" column="status"/>
        <result property="viewCount" column="view_count"/>
        <result property="likeCount" column="like_count"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="publishedAt" column="published_at"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, agent_id, name, description, content, summary, tags,
        status, view_count, like_count, created_at, updated_at, published_at
    </sql>

    <insert id="insert" parameterType="com.agenthub.infrastructure.entity.AgentCardPO">
        INSERT INTO agent_cards (id, agent_id, name, description, content, summary, tags,
            status, view_count, like_count, created_at, updated_at, published_at)
        VALUES (#{id}, #{agentId}, #{name}, #{description}, #{content}, #{summary}, #{tags},
            #{status}, #{viewCount}, #{likeCount}, #{createdAt}, #{updatedAt}, #{publishedAt})
    </insert>

    <update id="update" parameterType="com.agenthub.infrastructure.entity.AgentCardPO">
        UPDATE agent_cards
        SET name = #{name},
            description = #{description},
            content = #{content},
            summary = #{summary},
            tags = #{tags},
            status = #{status},
            view_count = #{viewCount},
            like_count = #{likeCount},
            updated_at = #{updatedAt},
            published_at = #{publishedAt}
        WHERE id = #{id}
    </update>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM agent_cards WHERE id = #{id}
    </select>

    <select id="selectByAgentId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM agent_cards WHERE agent_id = #{agentId}
    </select>

    <select id="selectByStatus" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM agent_cards WHERE status = #{status}
        ORDER BY created_at DESC
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM agent_cards ORDER BY created_at DESC
    </select>

    <select id="selectByPage" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM agent_cards ORDER BY created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="search" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM agent_cards
        WHERE name LIKE CONCAT('%', #{keyword}, '%')
           OR description LIKE CONCAT('%', #{keyword}, '%')
           OR tags LIKE CONCAT('%', #{keyword}, '%')
        ORDER BY created_at DESC
    </select>

    <select id="searchWithPage" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM agent_cards
        WHERE name LIKE CONCAT('%', #{keyword}, '%')
           OR description LIKE CONCAT('%', #{keyword}, '%')
           OR tags LIKE CONCAT('%', #{keyword}, '%')
        ORDER BY created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="selectByStatusAndPage" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM agent_cards WHERE status = #{status}
        ORDER BY created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="count" resultType="long">
        SELECT COUNT(*) FROM agent_cards
    </select>

    <select id="countByStatus" resultType="long">
        SELECT COUNT(*) FROM agent_cards WHERE status = #{status}
    </select>

    <select id="countBySearch" resultType="long">
        SELECT COUNT(*) FROM agent_cards
        WHERE name LIKE CONCAT('%', #{keyword}, '%')
           OR description LIKE CONCAT('%', #{keyword}, '%')
           OR tags LIKE CONCAT('%', #{keyword}, '%')
    </select>

    <delete id="deleteById">
        DELETE FROM agent_cards WHERE id = #{id}
    </delete>

    <update id="incrementViewCount">
        UPDATE agent_cards SET view_count = view_count + 1 WHERE id = #{id}
    </update>

    <update id="incrementLikeCount">
        UPDATE agent_cards SET like_count = like_count + 1 WHERE id = #{id}
    </update>
</mapper>
